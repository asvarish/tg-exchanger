export enum UserState {
  START = 'start',
  ENTERING_AMOUNT = 'entering_amount',
  ENTERING_CITY = 'entering_city',
  WAITING_ADMIN_RESPONSE = 'waiting_admin_response',
  WAITING_COMPLETION_LINK = 'waiting_completion_link',
}
